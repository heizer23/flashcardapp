<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <!-- WRITE_EXTERNAL_STORAGE has no effect on API 29+; kept only for API 28 and below -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material3.DayNight">

        <!-- Main Activity -->
        <activity
            android:name=".main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other Activities -->
        <activity android:name=".main.AddFlashcardActivity" />
        <activity android:name=".main.ListFlashcardsActivity" />
        <activity android:name=".main.ReviewFlashcardsActivity" />
        <activity android:name=".main.EditFlashcardActivity" />
        <activity android:name=".main.ImportFlashcardsActivity" />
        <activity android:name=".main.TopicSelectionActivity" />
        <activity android:name=".main.ChatGPTActivity" />
        <activity android:name=".main.GenerateQuestionsActivity" />

    </application>
</manifest>
