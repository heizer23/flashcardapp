<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.flashcardapp">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material3.DayNight">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other Activities -->
        <activity android:name=".AddFlashcardActivity" />
        <activity android:name=".ListFlashcardsActivity" />
        <activity android:name=".ReviewFlashcardsActivity" />
        <activity android:name=".EditFlashcardActivity" />
        <activity android:name=".ImportFlashcardsActivity" />
        <activity android:name=".TopicSelectionActivity" />
        <activity android:name=".ChatGPTActivity" />
        <activity android:name=".GenerateQuestionsActivity" />

    </application>
</manifest>
