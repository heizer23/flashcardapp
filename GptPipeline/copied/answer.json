{
  "explanation": "Added a new query in FlashcardDao to fetch all flashcards for selected topics, added a matching repository method, and introduced a 'Show All' button in ListFlashcardsActivity. This preserves existing functionality and introduces a minimal new function to see all questions.",
  "code_changes": {
    "com.example.flashcardapp.data": {
      "FlashcardDao": {
        "file_path": "C:\\Users\\premm\\StudioProjects\\flashcardapp\\app\\src\\main\\java\\com\\example\\flashcardapp\\data\\FlashcardDao.kt",
        "content": "package com.example.flashcardapp.data\n\nimport androidx.room.*\n\n@Dao\ninterface FlashcardDao {\n\n    @Insert(onConflict = OnConflictStrategy.REPLACE)\n    fun insertFlashcard(flashcard: Flashcard): Long\n\n    @Update\n    fun updateFlashcard(flashcard: Flashcard)\n\n    @Delete\n    fun deleteFlashcard(flashcard: Flashcard)\n\n    @Query(\"SELECT * FROM flashcards WHERE id = :id\")\n    fun getFlashcardById(id: Int): Flashcard?\n\n    @Query(\"SELECT * FROM flashcards ORDER BY nextReview ASC\")\n    fun getAllFlashcards(): List<Flashcard>\n\n    @Query(\"SELECT * FROM flashcards WHERE nextReview <= :currentTime ORDER BY nextReview DESC LIMIT 1\")\n    fun getNextDueFlashcard(currentTime: Long): Flashcard?\n\n    @Query(\"SELECT * FROM flashcards WHERE nextReview > :currentTime ORDER BY nextReview ASC\")\n    fun getFutureFlashcards(currentTime: Long): List<Flashcard>\n\n    @Query(\"SELECT * FROM flashcards WHERE nextReview <= :currentTime ORDER BY nextReview DESC\")\n    fun getPastFlashcards(currentTime: Long): List<Flashcard>\n\n    @Insert(onConflict = OnConflictStrategy.IGNORE)\n    fun insertTopic(topic: Topic): Long\n\n    @Query(\"SELECT * FROM topics\")\n    fun getAllTopics(): List<Topic>\n\n    @Query(\"SELECT * FROM topics WHERE name = :topicName LIMIT 1\")\n    fun getTopicByName(topicName: String): Topic?\n\n    @Query(\"UPDATE topics SET selected = :isSelected WHERE id = :topicId\")\n    fun updateTopicSelection(topicId: Int, isSelected: Boolean)\n\n    @Insert(onConflict = OnConflictStrategy.IGNORE)\n    fun insertCrossRef(crossRef: FlashcardTopicCrossRef)\n\n    // 1) We'll add a query to delete old crossRefs\n    @Query(\"DELETE FROM flashcard_topic_cross_ref WHERE flashcardId = :flashcardId\")\n    fun deleteCrossRefsForFlashcard(flashcardId: Int)\n\n    // 2) We'll add a query to actually get all topics for that flashcard\n    @Query(\"SELECT t.* FROM topics t INNER JOIN flashcard_topic_cross_ref fcr ON t.id = fcr.topicId WHERE fcr.flashcardId = :flashcardId\")\n    fun getTopicsForFlashcard(flashcardId: Int): List<Topic>\n\n    @Query(\"SELECT COUNT(*) FROM flashcards WHERE nextReview <= :currentTime\")\n    fun getPastCount(currentTime: Long): Int\n\n    @Query(\"SELECT COUNT(*) FROM flashcards WHERE nextReview > :currentTime\")\n    fun getFutureCount(currentTime: Long): Int\n\n    // NEW QUERIES: Only selected topics\n    @Query(\"SELECT DISTINCT f.* FROM flashcards f\\n           JOIN flashcard_topic_cross_ref xref ON xref.flashcardId = f.id\\n           JOIN topics t ON t.id = xref.topicId\\n           WHERE t.selected = 1 AND f.nextReview <= :currentTime\\n           ORDER BY f.nextReview DESC\")\n    fun getPastFlashcardsForSelectedTopics(currentTime: Long): List<Flashcard> // changes: update\n\n    @Query(\"SELECT DISTINCT f.* FROM flashcards f\\n           JOIN flashcard_topic_cross_ref xref ON xref.flashcardId = f.id\\n           JOIN topics t ON t.id = xref.topicId\\n           WHERE t.selected = 1 AND f.nextReview > :currentTime\\n           ORDER BY f.nextReview ASC\")\n    fun getFutureFlashcardsForSelectedTopics(currentTime: Long): List<Flashcard> // changes: update\n\n    // changes: update\n    @Query(\"SELECT DISTINCT f.* FROM flashcards f\\n           JOIN flashcard_topic_cross_ref xref ON xref.flashcardId = f.id\\n           JOIN topics t ON t.id = xref.topicId\\n           WHERE t.selected = 1\\n           ORDER BY f.nextReview ASC\")\n    fun getAllFlashcardsForSelectedTopics(): List<Flashcard>\n}\n",
        "changes": "update"
      },
      "FlashcardRepository": {
        "file_path": "C:\\Users\\premm\\StudioProjects\\flashcardapp\\app\\src\\main\\java\\com\\example\\flashcardapp\\data\\FlashcardRepository.kt",
        "content": "package com.example.flashcardapp.data\n\nimport kotlinx.coroutines.Dispatchers\nimport kotlinx.coroutines.withContext\n\n/**\n * Consolidated repository in package com.example.flashcardapp.data.\n * We merged any missing methods from the old repository, e.g., createFlashcard, associateFlashcardWithTopic, getPastAndFutureQuestionsCount.\n */\nclass FlashcardRepository(private val flashcardDao: FlashcardDao) {\n\n    // 1) createFlashcard - returns the inserted Flashcard with updated ID\n    suspend fun createFlashcard(flashcard: Flashcard): Flashcard = withContext(Dispatchers.IO) {\n        val newId = flashcardDao.insertFlashcard(flashcard)\n        flashcard.id = newId.toInt()\n        flashcard\n    }\n\n    // 2) Insert an existing flashcard, returning the rowId\n    suspend fun insertFlashcard(flashcard: Flashcard): Long = withContext(Dispatchers.IO) {\n        flashcardDao.insertFlashcard(flashcard)\n    }\n\n    // 3) Update\n    suspend fun updateFlashcard(flashcard: Flashcard) = withContext(Dispatchers.IO) {\n        flashcardDao.updateFlashcard(flashcard)\n    }\n\n    // 4) Delete\n    suspend fun deleteFlashcard(flashcard: Flashcard) = withContext(Dispatchers.IO) {\n        flashcardDao.deleteFlashcard(flashcard)\n    }\n\n    // 5) Retrieve by ID\n    suspend fun getFlashcardById(id: Int): Flashcard? = withContext(Dispatchers.IO) {\n        flashcardDao.getFlashcardById(id)\n    }\n\n    // 6) Retrieve all\n    suspend fun getAllFlashcards(): List<Flashcard> = withContext(Dispatchers.IO) {\n        flashcardDao.getAllFlashcards()\n    }\n\n    // 7) Next due flashcard\n    suspend fun getNextDueFlashcard(currentTime: Long): Flashcard? = withContext(Dispatchers.IO) {\n        flashcardDao.getNextDueFlashcard(currentTime)\n    }\n\n    // 8) Future flashcards\n    suspend fun getFutureFlashcards(currentTime: Long): List<Flashcard> = withContext(Dispatchers.IO) {\n        flashcardDao.getFutureFlashcards(currentTime)\n    }\n\n    // 9) Past flashcards\n    suspend fun getPastFlashcards(currentTime: Long): List<Flashcard> = withContext(Dispatchers.IO) {\n        flashcardDao.getPastFlashcards(currentTime)\n    }\n\n    // 10) Insert a topic\n    suspend fun insertTopic(topic: Topic): Topic = withContext(Dispatchers.IO) {\n        val existing = flashcardDao.getTopicByName(topic.name)\n        if (existing != null) {\n            existing\n        } else {\n            val newId = flashcardDao.insertTopic(topic)\n            topic.id = newId.toInt()\n            topic\n        }\n    }\n\n    // 11) Get all topics\n    suspend fun getAllTopics(): List<Topic> = withContext(Dispatchers.IO) {\n        flashcardDao.getAllTopics()\n    }\n\n    // 12) Get topic by name\n    suspend fun getTopicByName(topicName: String): Topic? = withContext(Dispatchers.IO) {\n        flashcardDao.getTopicByName(topicName)\n    }\n\n    // 13) Update topic's selected state\n    suspend fun updateTopicSelection(topicId: Int, isSelected: Boolean) = withContext(Dispatchers.IO) {\n        flashcardDao.updateTopicSelection(topicId, isSelected)\n    }\n\n    // 14) Associate flashcard with topic\n    suspend fun associateFlashcardWithTopic(flashcardId: Int, topicId: Int) = withContext(Dispatchers.IO) {\n        flashcardDao.insertCrossRef(FlashcardTopicCrossRef(flashcardId, topicId))\n    }\n\n    // 15) Combine past/future counts\n    suspend fun getPastAndFutureQuestionsCount(currentTime: Long): IntArray = withContext(Dispatchers.IO) {\n        val past = flashcardDao.getPastCount(currentTime)\n        val future = flashcardDao.getFutureCount(currentTime)\n        intArrayOf(past, future)\n    }\n\n    // NEW METHODS: filter by selected topics\n    suspend fun getPastFlashcardsForSelectedTopics(currentTime: Long): List<Flashcard> = withContext(Dispatchers.IO) {\n        flashcardDao.getPastFlashcardsForSelectedTopics(currentTime)\n    } // changes: update\n\n    suspend fun getFutureFlashcardsForSelectedTopics(currentTime: Long): List<Flashcard> = withContext(Dispatchers.IO) {\n        flashcardDao.getFutureFlashcardsForSelectedTopics(currentTime)\n    } // changes: update\n\n    // changes: update\n    suspend fun getAllFlashcardsForSelectedTopics(): List<Flashcard> = withContext(Dispatchers.IO) {\n        flashcardDao.getAllFlashcardsForSelectedTopics()\n    }\n}\n",
        "changes": "update"
      }
    },
    "com.example.flashcardapp.main": {
      "ListFlashcardsActivity": {
        "file_path": "C:\\Users\\premm\\StudioProjects\\flashcardapp\\app\\src\\main\\java\\com\\example\\flashcardapp\\main\\ListFlashcardsActivity.kt",
        "content": "package com.example.flashcardapp.main\n\nimport android.os.Bundle\nimport android.widget.Button  // changes: update\nimport android.widget.TextView\nimport android.widget.ToggleButton\nimport androidx.appcompat.app.AppCompatActivity\nimport androidx.lifecycle.lifecycleScope\nimport androidx.recyclerview.widget.LinearLayoutManager\nimport androidx.recyclerview.widget.RecyclerView\nimport com.example.flashcardapp.R\nimport com.example.flashcardapp.data.Flashcard\nimport com.example.flashcardapp.data.FlashcardRepository\nimport com.example.flashcardapp.data.FlashcardRoomDatabase\nimport kotlinx.coroutines.Dispatchers\nimport kotlinx.coroutines.launch\nimport kotlinx.coroutines.withContext\n\n// We'll define a small enum for readability:\nprivate enum class SortField { QUESTION, ANSWER, NEXT_REVIEW, INTERVAL }\n\nclass ListFlashcardsActivity : AppCompatActivity() {\n\n    private lateinit var toggleReviewTime: ToggleButton\n    private lateinit var flashcardRepository: FlashcardRepository\n    private var flashcards: MutableList<Flashcard> = mutableListOf()\n    private lateinit var flashcardAdapter: FlashcardAdapter\n\n    // NEW: references to the clickable \"column headers\"\n    private lateinit var tvHeaderQuestion: TextView\n    private lateinit var tvHeaderAnswer: TextView\n    private lateinit var tvHeaderTime: TextView\n    private lateinit var tvHeaderInterval: TextView\n\n    // Track our sort preference\n    private var currentSortField: SortField = SortField.NEXT_REVIEW\n    private var ascending: Boolean = true\n\n    // changes: update (add a new button)\n    private lateinit var btnShowAll: Button\n\n    override fun onCreate(savedInstanceState: Bundle?) {\n        super.onCreate(savedInstanceState)\n        setContentView(R.layout.activity_list_flashcards)\n\n        tvHeaderQuestion = findViewById(R.id.tv_header_question)\n        tvHeaderAnswer   = findViewById(R.id.tv_header_answer)\n        tvHeaderTime     = findViewById(R.id.tv_header_time)\n        tvHeaderInterval = findViewById(R.id.tv_header_interval)\n\n        tvHeaderQuestion.setOnClickListener {\n            if (currentSortField == SortField.QUESTION) {\n                ascending = !ascending\n            } else {\n                currentSortField = SortField.QUESTION\n                ascending = true\n            }\n            sortAndRefresh()\n        }\n        tvHeaderAnswer.setOnClickListener {\n            if (currentSortField == SortField.ANSWER) {\n                ascending = !ascending\n            } else {\n                currentSortField = SortField.ANSWER\n                ascending = true\n            }\n            sortAndRefresh()\n        }\n        tvHeaderTime.setOnClickListener {\n            if (currentSortField == SortField.NEXT_REVIEW) {\n                ascending = !ascending\n            } else {\n                currentSortField = SortField.NEXT_REVIEW\n                ascending = true\n            }\n            sortAndRefresh()\n        }\n        tvHeaderInterval.setOnClickListener {\n            if (currentSortField == SortField.INTERVAL) {\n                ascending = !ascending\n            } else {\n                currentSortField = SortField.INTERVAL\n                ascending = true\n            }\n            sortAndRefresh()\n        }\n\n        toggleReviewTime = findViewById(R.id.toggle_review_time)\n\n        // changes: update (initialize new button)\n        btnShowAll = findViewById(R.id.btn_show_all)\n\n        flashcardRepository = FlashcardRepository(\n            FlashcardRoomDatabase.getDatabase(applicationContext).flashcardDao()\n        )\n\n        flashcardAdapter = FlashcardAdapter(this, flashcards)\n        val recyclerView = findViewById<RecyclerView>(R.id.recycler_view_flashcards)\n        recyclerView.layoutManager = LinearLayoutManager(this)\n        recyclerView.adapter = flashcardAdapter\n\n        // Load initial data (future questions) but only where topics are selected\n        loadFlashcards(true)\n\n        toggleReviewTime.setOnCheckedChangeListener { _, isChecked ->\n            if (isChecked) {\n                // Show past questions with selected topics\n                loadFlashcards(false)\n            } else {\n                // Show future questions with selected topics\n                loadFlashcards(true)\n            }\n        }\n\n        // changes: update\n        // When user clicks \"Show All\" we load all flashcards (selected topics) ignoring nextReview.\n        btnShowAll.setOnClickListener {\n            loadAllFlashcards()\n        }\n    }\n\n    private fun loadFlashcards(showFuture: Boolean) {\n        lifecycleScope.launch(Dispatchers.IO) {\n            val list: List<Flashcard> = if (showFuture) {\n                flashcardRepository.getFutureFlashcardsForSelectedTopics(System.currentTimeMillis())\n            } else {\n                flashcardRepository.getPastFlashcardsForSelectedTopics(System.currentTimeMillis())\n            }\n            withContext(Dispatchers.Main) {\n                flashcards.clear()\n                flashcards.addAll(list)\n                sortAndRefresh()\n            }\n        }\n    }\n\n    // changes: update (new function)\n    private fun loadAllFlashcards() {\n        lifecycleScope.launch(Dispatchers.IO) {\n            val list = flashcardRepository.getAllFlashcardsForSelectedTopics()\n            withContext(Dispatchers.Main) {\n                flashcards.clear()\n                flashcards.addAll(list)\n                sortAndRefresh()\n            }\n        }\n    }\n\n    private fun sortAndRefresh() {\n        when (currentSortField) {\n            SortField.QUESTION -> {\n                if (ascending) {\n                    flashcards.sortBy { it.question }\n                } else {\n                    flashcards.sortByDescending { it.question }\n                }\n            }\n            SortField.ANSWER -> {\n                if (ascending) {\n                    flashcards.sortBy { it.answer }\n                } else {\n                    flashcards.sortByDescending { it.answer }\n                }\n            }\n            SortField.NEXT_REVIEW -> {\n                if (ascending) {\n                    flashcards.sortBy { it.nextReview }\n                } else {\n                    flashcards.sortByDescending { it.nextReview }\n                }\n            }\n            SortField.INTERVAL -> {\n                if (ascending) {\n                    flashcards.sortBy { it.interval }\n                } else {\n                    flashcards.sortByDescending { it.interval }\n                }\n            }\n        }\n        flashcardAdapter.notifyDataSetChanged()\n    }\n\n    override fun onDestroy() {\n        super.onDestroy()\n    }\n}\n",
        "changes": "update"
      }
    }
  }
}
